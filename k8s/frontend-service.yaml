apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80        # exposed externally
      targetPort: 80  # container port
  type: LoadBalancer
